# A-Frame AR.js Marker Example

## Run development

Install:
- `npm install`: install node dependencies

Run: 
- `npm run dev`: builds ts into js and serves a local http instance
- `npm run https`: serves the built js project as https
